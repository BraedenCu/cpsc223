void SelectionSort(int numbers[], int numbersSize) {
   int i, j;
   int currentElement;

   for (i = 1; i < numbersSize; i++) {
      currentElement = numbers[i];
      j = i - 1;

      //TODO: Implement the rest of this function here. Need another loop uses j as the index variable. I used a while loop in my solution.

      // Move elements of numbers[0..i-1], that are greater than currentElement,
      // to one position ahead of their current position
   }
}
